import React from "react";
import {
  TextField,
  Stack,
  Typography,
  Button,
  Dialog,
  DialogActions,
  DialogContent,
  DialogTitle,
  IconButton,
  Rating,
  MenuItem,
  Alert,
  Grid,
} from "@mui/material";

const Maps = () => {
  return (
    <Grid>
      <Typography>Google maps location</Typography>
    </Grid>
  );
};

export default Maps;
